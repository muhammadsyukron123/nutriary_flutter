import 'package:flutter/material.dart';

class AddFoodLogDialog extends StatelessWidget {
  const AddFoodLogDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Placeholder(),
    );
  }
}
